const User = require('../models/userModel');

async function user_google({ name, email, picture }) {
    let user = await User.findOne({ email });
    if (!user) {
        user = await User.create({
            name,
            email,
            image: picture,
        });
    }
    return user;
}

module.exports = { user_google };
